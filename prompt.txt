Your task: Fix the PVC naming mismatch in the `charts/mlflow` chart

Requirements:

1. Unify the PVC template metadata name and the workload's `persistentVolumeClaim.claimName` so they render to the same value.
2. The PVC name rule must be exactly `{{ include "mlflow.fullname" . }}-data-pvc`.
3. Keep the volume and volume mount definitions aligned with that PVC and with the configured `persistence.mountPath`.
